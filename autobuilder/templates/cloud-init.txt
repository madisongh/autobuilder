#cloud-config
fs_setup:
    - label: SCRATCH
      filesystem: ext4
      device: ephemeral0
      partition: auto

write_files:
    - content: |
        WORKERNAME="{{ workername }}"
        WORKERSECRET="{{ workersecret }}"
        MASTER="{{ master_ip }}"
      path: /etc/default/buildworker
      permissions: '0644'

runcmd:
    - wget https://d1b0l86ne08fsf.cloudfront.net/mender-cli/1.2.0/mender-cli -O /usr/local/bin/mender-cli
    - chmod +x /usr/local/bin/mender-cli


